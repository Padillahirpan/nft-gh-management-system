{
  "name": "nft-greenhouse",
  "version": "1.0.0",
  "private": true,
  "description": "NFT Greenhouse Management System - Platform manajemen kebun selada bokor berbasis Nutrient Film Technique",
  "scripts": {
    "dev": "concurrently \"pnpm --filter backend dev\" \"pnpm --filter frontend dev\"",
    "dev:backend": "pnpm --filter backend dev",
    "dev:frontend": "pnpm --filter frontend dev",
    "build": "pnpm --filter backend build && pnpm --filter frontend build",
    "build:backend": "pnpm --filter backend build",
    "build:frontend": "pnpm --filter frontend build",
    "lint": "pnpm --filter backend lint && pnpm --filter frontend lint",
    "test": "pnpm --filter backend test && pnpm --filter frontend test",
    "clean": "rm -rf node_modules apps/*/node_modules apps/*/dist"
  },
  "keywords": [
    "nft",
    "greenhouse",
    "hydroponics",
    "iot",
    "monitoring"
  ],
  "author": "Greenhouse Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  }
}
